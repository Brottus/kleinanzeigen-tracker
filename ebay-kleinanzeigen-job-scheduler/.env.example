# Job Scheduler - Environment Variables

# Server Configuration
PORT=3001
SESSION_SECRET=change-this-to-a-random-secret-key
JWT_SECRET=change-this-to-a-different-random-secret-key

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Controls Python logging verbosity - SAFE to use DEBUG in production for troubleshooting
# Default: INFO (if not set)
LOG_LEVEL=INFO

# Flask Debug Mode Configuration
# SECURITY WARNING: NEVER set to true in production!
# Flask debug enables interactive debugger, auto-reloader, and detailed error pages
# Use LOG_LEVEL=DEBUG instead for safe production debugging
# Default: false (if not set)
FLASK_DEBUG=false

# UI Configuration
# Enable/disable Swagger API documentation at /docs
# Default: true (enabled)
ENABLE_SWAGGER_UI=true

# Enable/disable web dashboard at /
# Set to false for API-only mode (recommended when using external UI)
# Default: true (enabled)
ENABLE_WEB_UI=true

# Admin User (created on first startup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Database
DB_PATH=jobs.db

# Scraper API Configuration (can be changed via API or database)
SCRAPER_API_URL=http://localhost:3000
SCRAPER_API_KEY=test-key-123
SCRAPER_REQUEST_TIMEOUT=30

# Matterbridge Integration (optional)
# MATTERBRIDGE_URL=http://matterbridge:4242
# MATTERBRIDGE_TOKEN=your-matterbridge-token
# MATTERBRIDGE_GATEWAY=gateway_ebaykleinanzeigen
# MATTERBRIDGE_USERNAME=Kleinanzeigen Bot

# Apprise Notification API (optional)
# Deploy the official Apprise API container: docker pull caronc/apprise
# Configure notification URLs in ./apprise-config/apprise.yml
# APPRISE_API_URL=http://apprise:8000
# APPRISE_API_KEY=kleinanzeigen
# Only needed if Apprise is behind a reverse proxy with HTTP Basic Auth:
# APPRISE_USERNAME=
# APPRISE_PASSWORD=
